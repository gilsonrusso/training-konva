{
  "compilerOptions": {
    "target": "es6",                          /* Especifica o destino da versão ECMAScript. */
    "module": "commonjs",                     /* Especifica o sistema de módulos a ser gerado. */
    "outDir": "./dist",                       /* Especifica o diretório de saída para os arquivos JavaScript compilados. */
    "strict": true,                           /* Habilita todas as opções de verificação de tipo estrita. */
    "esModuleInterop": true,                  /* Habilita a interoperabilidade entre módulos CommonJS e ES modules. */
    "skipLibCheck": true,                     /* Ignora a verificação de tipo de arquivos de declaração (.d.ts). */
    "forceConsistentCasingInFileNames": true, /* Garante que os nomes de arquivos não diferem apenas no caso. */
    "resolveJsonModule": true,                /* Permite importar módulos com extensão .json. */
    "sourceMap": true                         /* Gera arquivos de mapa de origem correspondentes. */
  },
  "include": [
    "**/*.ts"                                 /* Inclui todos os arquivos .ts na pasta atual e subpastas. */
  ],
  "exclude": [
    "node_modules",                           /* Exclui a pasta node_modules. */
    "**/*.spec.ts"                            /* Exclui arquivos de teste. */
  ]
}