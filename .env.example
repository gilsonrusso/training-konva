# Variáveis de Ambiente
# Prefixo REACT_APP_ é necessário para aplicações Create React App (CRA)
# Para Vite, Next.js, etc., o prefixo pode ser VITE_ ou NEXT_PUBLIC_
# Ajuste conforme o seu framework. Se não estiver usando um framework específico,
# verifique como ele expõe variáveis de ambiente ao lado do cliente.

VITE_API_BASE_URL=http://localhost:5000/api